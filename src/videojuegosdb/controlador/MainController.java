/* Controlador para la ventana principal. */
package videojuegosdb.controlador;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;

/**
 * Controlador para la ventana principal.
 *
 * @author Víctor Zamora Gutiérrez
 * @version 1.0
 */
public class MainController implements Initializable {

    /**
     * Inicializa la ventana principal (descripciones de los parámetros 
     * copiadas directamente de la clase Initializable.
     * @param url - The location used to resolve relative paths for 
     * the root object, or null if the location is not known.
     * @param rb - The resources used to localize the root object, 
     * or null if the root object was not localized.
     */
    @Override
    public void initialize(URL url, ResourceBundle rb) {
    }    
    
}
